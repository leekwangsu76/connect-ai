# ============================================================
# 뽀족랭킹 데이터 파이프라인 설정 파일
# 이 파일만 수정하면 모든 동작을 조절할 수 있습니다.
# ============================================================
import os


# ─────────────────────────────────────
# 1. 네이버 API 키 (건드리지 마세요)
# ─────────────────────────────────────
NAVER_CLIENT_ID     = "283RWIb4IRT3mzUb2PTL"  # 네이버 개발자센터에서 발급받은 ID
NAVER_CLIENT_SECRET = "VpFMWwe1ZS"             # 네이버 개발자센터에서 발급받은 비밀키


# ─────────────────────────────────────
# 2. AI 모델 설정
# ─────────────────────────────────────
OLLAMA_URL = "http://localhost:11434"  # Ollama 주소 (보통 건드릴 필요 없음)

# 사용할 AI 모델을 선택하세요. 아래 중 하나를 골라 붙여넣으면 됩니다.
#   "gemma4:e2b"       → 가장 작고 빠름. 간단한 내용 정리에 적합. 정확도 낮음.
#   "gemma4:e4b"       → 중간 크기. 속도와 정확도의 균형. 현재 추천값.
#   "gemma4-ks:latest" → 한국어에 특화된 모델. 한국어 리뷰 분석에 유리.
OLLAMA_MODEL = "gemma4:e4b"

# AI의 창의성 조절 (0.0 ~ 1.0 사이 숫자)
#   0.0 에 가까울수록 → 정해진 사실만 말함 (데이터 정리용 추천)
#   1.0 에 가까울수록 → 창의적으로 지어냄 (글쓰기용)
#   ※ 데이터 비교표 생성이므로 0.1 고정 추천
OLLAMA_TEMPERATURE = 0.1


# ─────────────────────────────────────
# 3. 데이터 수집 옵션
# ─────────────────────────────────────

# 장소 1곳당 수집할 블로그 리뷰 개수
#   많을수록 → 정보가 풍부해짐, 분석 시간 길어짐
#   적을수록 → 빠르지만 정보 부족할 수 있음
#   추천 범위: 5 ~ 20
BLOG_REVIEW_COUNT = 10

# 네이버 지역 검색 결과 수 (장소 기본 정보용)
#   보통 3~5개면 충분. 1개만 써도 무방.
LOCAL_SEARCH_COUNT = 5


# ─────────────────────────────────────
# 4. AI 분석 처리 방식 옵션 ← 여기서 테스트하세요!
# ─────────────────────────────────────

# [옵션 A] 처리 방식 (PROCESS_MODE)
#   "all_at_once" → 모든 장소 데이터를 한꺼번에 AI에게 전달
#                   장점: 빠름
#                   단점: AI가 너무 많은 내용을 받아 헷갈려할 수 있음
#
#   "one_by_one"  → 장소 1개씩 AI가 분석 후 마지막에 합침
#                   장점: AI 부담이 줄어 정확도 높음
#                   단점: 시간이 장소 수만큼 걸림 (3곳이면 3배)
PROCESS_MODE = "one_by_one"


# [옵션 B] 리뷰 전달 방식 (REVIEW_MODE)
#   "full"    → 블로그 리뷰 전체 내용을 AI에게 전달
#               장점: 정보가 많아 AI가 다양하게 분석 가능
#               단점: AI 처리 부하가 크고 핵심을 놓칠 수 있음
#
#   "summary" → Python이 리뷰에서 앞부분만 잘라 전달 (빠른 요약)
#               장점: AI 부담이 적고 빠름
#               단점: 뒷부분의 중요한 정보가 잘릴 수 있음
REVIEW_MODE = "summary"


# [옵션 C] 리뷰 요약 글자 수 (REVIEW_MODE = "summary" 일 때만 적용)
#   숫자가 클수록 → AI에게 더 많은 내용 전달 (정보 풍부, 처리 느림)
#   숫자가 작을수록 → AI에게 핵심만 전달 (처리 빠름, 정보 적음)
#   추천 범위: 100 ~ 500
REVIEW_SUMMARY_LENGTH = 300


# ─────────────────────────────────────
# 5. 저장 경로 (건드리지 마세요)
# ─────────────────────────────────────
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DATA_DIR = os.path.join(BASE_DIR, "data")  # 수집 데이터가 저장되는 폴더
